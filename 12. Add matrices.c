#include <stdio.h>

void main()
{
    int rN, cN, i=0, j=0;
    printf("Enter the number of rows: ");
    scanf("%d", &rN);
    printf("Enter the number of columns: ");
    scanf("%d", &cN);

    for(i = 0; i < rN; i++)
    {
        for(j = 0; j< cN; j++)
        {

        }
    }
}
